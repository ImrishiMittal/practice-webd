import { StyleSheet } from "react-native";

const Exstyle = StyleSheet.create({
  textColor: {
    color: "white",
    fontSize: 25,
    backgroundColor: "#271585",
    borderRadius: 50,
    padding: 5,
    textAlign: "center",
    borderColor: "#00FF04",
    borderWidth: 2,
  },
});

export default Exstyle;